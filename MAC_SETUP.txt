═══════════════════════════════════════════════════════════════
      הוראות להפעלה Game Project על Mac
═══════════════════════════════════════════════════════════════

🔧 דרישות מוקדמות:
  • Node.js 16+ (בדוק: node --version)
  • Python 3.8+ (בדוק: python3 --version)
  • Git (בדוק: git --version)

❌ אם python3 לא מותקן:
  1. התקן Homebrew:
     /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  
  2. התקן Python:
     brew install python@3.11
  
  3. וודא:
     python3 --version

═══════════════════════════════════════════════════════════════
      שלבים להפעלה
═══════════════════════════════════════════════════════════════

שלב 1: Clone את הפרויקט
──────────────────────────
$ git clone https://github.com/RotemD2b2t/Game.git
$ cd Game

שלב 2: התקן Python dependencies
──────────────────────────────────
$ python3 -m pip install -r requirements.txt

או:
$ pip3 install -r requirements.txt

שלב 3: התקן Node dependencies
────────────────────────────────
$ npm install

שלב 4: בנה את Frontend
────────────────────────
$ npm run build

שלב 5: הפעל את השרת
────────────────────────
בחר אחד מהשניים:

אפשרות A (מומלץ):
$ npm run dev

אפשרות B:
$ python3 main.py

שלב 6: גש לאתר
───────────────
פתח את הדפדפן וגש ל:
http://localhost:5000

═══════════════════════════════════════════════════════════════
      בדיקת Credentials בעת ההפעלה הראשונה
═══════════════════════════════════════════════════════════════

Username: RotemD
Password: admin123

(אל תשכח לשנות את הסיסמה בפרודקשן!)

═══════════════════════════════════════════════════════════════
      בעיות נפוצות
═══════════════════════════════════════════════════════════════

❓ "python: command not found"
✓ פתרון: השתמש ב- python3

❓ "pip: command not found"
✓ פתרון: השתמש ב- pip3 או python3 -m pip

❓ Port 5000 תפוס
✓ פתרון:
  lsof -i :5000        (בדוק מי תופס)
  kill -9 <PID>        (הרוג את התהליך)

❓ Database לא קיים
✓ פתרון: יוצר אוטומטית כשפותח את main.py

═══════════════════════════════════════════════════════════════
      בדיקת התכונות
═══════════════════════════════════════════════════════════════

1️⃣  Admin Dashboard:
   - התחבר כ- RotemD
   - גש ל- http://localhost:5000/admin

2️⃣  Ad Placeholders (אם admin עם premium):
   - בAdmin: לחץ "Toggle Premium" עבור RotemD
   - התחבר מחדש
   - אמור לראות מלבנים אפורים במקום המודעות

3️⃣  Daily Game Limit:
   - התחבר כ-user לא-premium
   - לחץ "שחק עכשיו"
   - משחק ראשון - אין ads
   - לחץ "שחק עכשיו" שוב - יופיע modal עם פרסומת

═══════════════════════════════════════════════════════════════
      צעדים נוספים
═══════════════════════════════════════════════════════════════

1. עדכן AdSense Slot IDs:
   client/src/lib/adConfig.ts

2. טסט על ה-published domain (Replit)

3. בדוק AdSense Dashboard לדוחות הכנסה

═══════════════════════════════════════════════════════════════
